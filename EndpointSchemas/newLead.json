{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "person_id": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9]+$",
        "description": "A unique identifier representing the person associated with the lead. Accepts alphanumeric value."
      },
      "calling_window": {
        "type": "array",
        "minItems": 7,
        "maxItems": 7,
        "items": {
          "type": "array",
          "items": {
            "type": "array",
            "items": [
              { "type": "integer", "minimum": 0 },
              { "type": "integer", "minimum": 0 }
            ]
          }
        },
        "description": "Time windows acceptable for calling, represented as a list of tuples for each day of the week."
      },
      "campaign_id": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9]+$",
        "description": "An identifier specifying the campaign to which the lead belongs. Accepts alphanumeric value."
      }
    },
    "required": ["person_id", "calling_window", "campaign_id"]
  }